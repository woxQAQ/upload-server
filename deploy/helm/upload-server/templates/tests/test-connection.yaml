apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upload-server.fullname" . }}-test-connection"
  labels:
    {{- include "upload-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upload-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
