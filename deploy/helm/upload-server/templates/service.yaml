apiVersion: v1
kind: Service
metadata:
  name: {{ include "upload-server.fullname" . }}
  labels:
    {{- include "upload-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upload-server.selectorLabels" . | nindent 4 }}
